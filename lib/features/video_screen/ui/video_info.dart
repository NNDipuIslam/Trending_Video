import 'package:flutter/material.dart';
import 'video_screen_ui.dart';

class videoInfo extends StatelessWidget {
  const videoInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
